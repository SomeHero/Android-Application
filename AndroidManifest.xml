<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.pdthx"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.SEND_SMS">
    </uses-permission>
    <uses-permission android:name="android.permission.RECEIVE_SMS">
    </uses-permission>
    <uses-sdk
        android:minSdkVersion="3"
        android:targetSdkVersion="4" />

    <application
        android:icon="@drawable/paidthx_icon"
        android:label="@string/app_name"
        android:debuggable="true">
        <activity
            android:label="@string/app_name"
            android:name=".SplashActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CustomTabActivity"
                  android:label="@string/app_name">
        </activity>
          <activity
            android:label="@string/app_name"
            android:name=".SignUpTabActivity" >
        </activity>
         <activity
            android:label="@string/app_name"
            android:name=".AcknowledgeDevice" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".SignInActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".HomeActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".MakePaymentActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".GetMoneyActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".RequestMoneyActivity" >
        </activity>
                <activity
            android:label="@string/app_name"
            android:name=".PaystreamActivity" >
        </activity>
    </application>

</manifest>